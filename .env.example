# Example environment variables for churn project
# Copy this file to `.env` and fill in real values

# .env.example
POSTGRES_DB=churn
POSTGRES_USER=churn_user
POSTGRES_PASSWORD=churn_pass

# Optional: app
APP_ENV=dev
